## What changed

<!-- One or two sentences. This branch is merged, not squashed, so every commit
     on it lands on `main` — each subject must follow Conventional Commits
     (`feat(scope): summary`), not just this title. -->

## How it was verified

<!-- e.g. `Pkg.test()` locally, or which pattern you played in the notebook. -->

- [ ] `julia --project=. -e 'using Pkg; Pkg.test()'` passes
- [ ] Every commit subject follows Conventional Commits, and the history is
      shaped the way it should read on `main`
- [ ] Notebook changes, if any, are in their own commit and were saved by Pluto
- [ ] No rendered audio or other large files added
